[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "/home/liunix/.local/bin/solc"
optimizer = true
optimizer_runs = 200

# VeriAgent / VeriPay — Arbitrum Open House Singapore Buildathon
# Chains: Arbitrum Sepolia (baseline) / Arbitrum One / Robinhood Chain testnet

[rpc_endpoints]
arbitrum_sepolia = "${ARBITRUM_SEPOLIA_RPC_URL}"
arbitrum_one = "${ARBITRUM_ONE_RPC_URL}"
